# Nika Website

## Run locally

```bash
npx serve .
```

Or with Python:

```bash
python3 -m http.server
```

Then open [http://localhost:3000](http://localhost:3000) (serve) or [http://localhost:8000](http://localhost:8000) (Python) in your browser.
